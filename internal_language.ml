(* Mario Alvarez - March 2013 *)
(* Implements data types and methods *)
(* Used internally in the compiler *)

module InternalLanguage = struct
	type chain_num = int;;
	type pred =
		| Srcip of  
		
end;;